# pass - the pass statement is a no-operation placeholder that is used when the syntax requires a statement.
# We can't leave the body empty without a syntax error, so we say pass instead.
def func1():
    pass  # Add real code here later

func1()  # nothing happens

class rec: pass  # Empty namespace object