# Launch Docker

```
$ cd vagrant
$ vagrant up
$ sudo -i # run as root
# docker images
REPOSITORY          TAG                 IMAGE ID            CREATED             SIZE
busybox             latest              00f017a8c2a6        3 weeks ago         1.11 MB
```