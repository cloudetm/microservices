//The COUNT function is used to count the number of rows returned by a query.
SELECT COUNT(*) FROM hotel.hotels;
//It can be used to count the number of non-null valus of a specified column.
SELECT COUNT(emails) FROM reservation.guests;
