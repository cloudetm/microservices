# Run commands from the bash shell

```
$ who
$ date
$ date -u
$ hostname
$ hostname -f
$ ls
$ ls /Users
$ ls Desktop
$ cd /Users
$ file /etc/hosts
$ ls -l
$ ls -ld Desktop
$ clear
$ bash --version
```